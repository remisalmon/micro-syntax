filetype: lookml

detect:
  filename: "\\.lkml$"

rules:
  - constant: "\\b[+-]?\\d+(\\.\\d*)?([eE][+-]?\\d+)?\\b"
  - constant: "\\b(yes|no)\\b"

  - special: "\\$\\{[^}]*\\}"

  - identifier: "^\\s*(-\\s+)?\\w+\\s*:"

  - constant.string:
      start: "\""
      end: "\""
      skip: "\\\\."
  - constant.string:
      start: "'"
      end: "'"
      skip: "\\\\."

  - comment: ";;"
  - comment:
      start: "#"
      end: "$"
